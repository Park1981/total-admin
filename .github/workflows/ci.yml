name: CI
on:
  push:
    branches: [ "**" ]
jobs:
  ci:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with: { node-version: '20' }
      - run: npm ci || npm i
      - run: npm run typegen || true
      - run: echo "?㎦ ?ш린??lint/test/e2e 異붽? 媛??
